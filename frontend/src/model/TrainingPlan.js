export function TrainingPlan(number, name, trainingType, personalTrainingsNumber, trainer, price) {

  this.number = number;
  this.name = name;
  this.trainingType = trainingType;
  this.personalTrainingsNumber = personalTrainingsNumber;
  this.trainer = trainer;
  this.price = price;
}